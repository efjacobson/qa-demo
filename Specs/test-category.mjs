import { assert } from 'chai';

describe('test-category', function() {
  it('validateCategoryById', function() {
    assert.strictEqual(1, 1);
  });
  it('validateCategoryById', function() {
    assert.strictEqual(1, 2);
  });
  it('someOtherTestName', function() {
    assert.strictEqual(1, 1);
  });
  it('whateverYouWant', function() {
    assert.strictEqual(1, 2);
  });
});